
BEGIN
    RETURN QUERY
    SELECT 
        u.id,
        u.username::VARCHAR(50),
        pgp_sym_decrypt(u.password, 'MasterEncryptionKey2024!')::TEXT,
        u.created_at
    FROM users u
    ORDER BY u.id;
END;
